# Roadmap

MoonRLLab is intentionally narrow in the first release so the core training loop stays readable.

Planned next steps:

- add more discrete environments
- add policy variants beyond epsilon-greedy
- add n-step learning and eligibility traces
- expose richer episode metrics
- add compact visualization helpers

